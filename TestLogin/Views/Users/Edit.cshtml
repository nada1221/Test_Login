@model TestLogin.Models.User

@{
	ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>User</h4>
<hr />
<div class="row">
	<div class="col-md-4">
		@*@using (Html.BeginForm(FormMethod.Po))
		{
			@Html.HiddenFor(m => m.Id)
			<div class="form-group">
				@Html.LabelFor(m => m.Pw, new { @class="control-label"})
				@Html.TextBoxFor(m => m.Pw, new { @class="form-control"})
				@Html.ValidationMessageFor(m => m.Pw)
			</div>
			<div>
				<button type="submit" class="btn btn-primary">저장</button>
			</div>
		}*@

		<form asp-action="Edit">
			<div asp-validation-summary="ModelOnly" class="text-danger"></div>
			<input type="hidden" asp-for="Id" />
			<div class="form-group">
				<label asp-for="Pw" class="control-label"></label>
				<input asp-for="Pw" class="form-control" />
				<span asp-validation-for="Pw" class="text-danger"></span>
			</div>
			<div class="form-group">
				<input type="submit" value="Save" class="btn btn-primary" />
			</div>
		</form>
	</div>
</div>

<div>
	<a asp-action="Index">Back to List</a>
</div>

